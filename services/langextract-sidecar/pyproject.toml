[project]
name = "singularity-langextract-sidecar"
version = "0.1.0"
description = "Stdio JSON sidecar wrapping Google LangExtract for Singularity Context Engine"
requires-python = ">=3.10"
dependencies = [
  "langextract[openai]==1.6.0",
]

[project.optional-dependencies]
dev = ["pytest>=8.0"]
