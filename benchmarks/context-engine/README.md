# Context Engine benchmarks

Run via the `@singularity/context` package tests:

```bash
npm run test -w @singularity/context
```

`METRICS.json` is regenerated on each benchmark test run with measured raw vs retrieved token estimates.
