# Outcome verification benchmarks

Run:

```bash
npm run test -w @singularity/outcome
```

`METRICS.json` is written by the TPS A–D test (100 concurrent mock coding ticks = `emit` + cache `lookup` only).
